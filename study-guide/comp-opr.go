package main

	import "fmt"
		
		func main() {
		 	var a, b int = 6, 7
			var s string = "kachi"
			var d string = "marviigrey"
			var f, k int = 30, 20 
			fmt.Println("is the value of s equal to d? \n", s == d)
			fmt.Println("is the value of s not equal to d? \n", s != d)
			fmt.Println("is a lesser than b? \n", a < b)
			fmt.Println("is f greater than k? \n", f > k)
		}
