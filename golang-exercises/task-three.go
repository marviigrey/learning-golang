package main

import "fmt"

func main() {
    var food = "Pizza"
    var slices = 4
    var pineappleOnPizza = true
    fmt.Printf("the data type of %v is: %T\n", food, food)
    fmt.Printf("for the %v, it is a %T \n", slices, slices)
    fmt.Printf("and lastly for %v is: %T \n", pineappleOnPizza, pineappleOnPizza)
    
}